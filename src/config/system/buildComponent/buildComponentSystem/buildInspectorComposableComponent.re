/* let buildInspector = (state_: option(AppStore.appState)) =>
  switch state_ {
  | Some(state) => <Inspector state />
  | _ => ExcepetionHandleSystem.throwMessage({j|inspector build:the arguments is error|j})
  }; */